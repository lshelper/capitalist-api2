# Merchant requests

Open these `.http` files in IntelliJ IDEA to read merchant orders.

- `orders.http` lists merchant orders.
- `orders-by-merchant.http` filters by merchant fields.
- `orders-by-order.http` filters by order fields.
