# Account requests

Open these `.http` files in IntelliJ IDEA to work with account data.

- `list.http` lists accounts by currency.
- `history.http` reads transaction history.
- `history-by-transaction-id.http` reads transaction history filtered by transaction ID.
