# Settings requests

Open these `.http` files in IntelliJ IDEA to manage API security settings exposed by the Integration API.

The public API currently exposes IP whitelist management. API access, API keys and API secrets are managed in the Capitalist personal account security settings, not through these HTTP endpoints.
